/* Object-set package
This uses a map datastructure under the hood.

The interface for ObjectSet should be identical to
https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set

Note: this set does not keep track of insertion order
*/

import ObjectSet from './object-set';

describe('Set constructor testing', () => {
  // Set can be passed an iterator as a constructor argument

  // Set can be passed an array as constructor argument

  // Set can be passed a set as a constructor argument

});

describe('Set length is accuratly updated', () => {
  // Set size after construction is accurate

  // Set size is updated correctly when items added

  // Set size is updated when items are deleted
});

describe('Set add testing', () => {

});

describe('Set clear testing', () => {
});

describe('set delete testing', () => {

});

describe('set .entries() returns an iterator', () => {

});

describe('set forEach(callbackFn[, thisArg]', () => {

});

describe('set .has(value) works correctly', () => {

});

describe('set keys()', () => {
  // Returns an array of objects
});

describe('set Symbol.iterator', () => {
  // Returns an array of Symbol iterator objects
});
